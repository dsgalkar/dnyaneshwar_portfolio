
/// Animation timing configurations
class AppAnimations {
  AppAnimations._();

  static const Duration instant = Duration(milliseconds: 150);
  static const Duration quick = Duration(milliseconds: 250);
  static const Duration standard = Duration(milliseconds: 400);
  static const Duration medium = Duration(milliseconds: 600);
  static const Duration slow = Duration(milliseconds: 900);
  static const Duration splashSequence = Duration(milliseconds: 2800);
  static const Duration backgroundLoop = Duration(seconds: 14);
}
